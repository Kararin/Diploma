import React from 'react';

const ScheduleDates  = ({
    startDate,
    endDate
}) => (
    <div>
        <div>
            {startDate} - {endDate}
        </div>
    </div>
)

export default ScheduleDates;